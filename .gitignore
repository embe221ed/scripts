package.json
package-lock.json

# Anchored at this repo's root, so `filter-repo --path configs` drops it and the
# dotfiles repo (where this file IS committed) never sees it. Keeps the file out
# of `scripts` for the 2.10 -> Phase 6 window in which it is still on disk here.
/configs/nvim/lazy-lock.json
